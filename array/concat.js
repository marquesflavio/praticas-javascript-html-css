const salaJS = ['Flávio', 'Evaldo', 'Gabi']

const salaPhyton = ['João', 'José', 'Pedro']

const salasUnificadas = salaJS.concat(salaPhyton)
console.log(salasUnificadas)

//concatenação de arrays, leva em consideração a ordem dos arrays