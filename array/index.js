const numeros = [1, 2, 3, 4]

const numerosX10 = numeros.map((callback) => {
    return callback * 10
//Outra forma de apresentar uma arrow function é assim:
    
//const numerosX10 = numeros.map((callback) => callback * 10) 

// tiramos o RETURN e as chaves
})
console.log(numerosX10)
